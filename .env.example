# Anthropic API key for Claude Code (required)
ANTHROPIC_API_KEY=sk-ant-...

# Context7 API key (optional, for higher rate limits)
# Get one at https://context7.com/dashboard
CONTEXT7_API_KEY=

# Path to the repository to mount into /workspace
# Defaults to . if not set
REPO_PATH=.
